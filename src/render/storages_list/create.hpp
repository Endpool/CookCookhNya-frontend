#pragma once

#include "render/common.hpp"

namespace cookcookhnya::render::storages_list {

void renderStorageCreation(ChatId chatId, UserId userId, BotRef bot);

} // namespace cookcookhnya::render::storages_list
